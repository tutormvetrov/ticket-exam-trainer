# Подробное руководство пользователя

Это руководство рассчитано на человека, который получил приложение уже собранным и не хочет разбираться в исходниках.
Если нужен запуск из исходников, сначала установите `Python 3.12+`, `PySide6` и зависимости проекта.

## Экран 1. Настройка локального ИИ

Откройте `Настройки -> Ollama`.

![Настройки Ollama](screenshots/settings.png)

Нижняя зона этого же экрана показывает реальную диагностику и сервисные действия.

![Нижняя зона настроек](screenshots/settings-bottom.png)

Что означает экран:
- `Адрес API` это локальный endpoint Ollama
- `Модель` это модель, которую приложение будет использовать
- `Папка с моделями` это каталог для локальных весов
- `Диагностика` показывает реальное состояние, а не декоративный статус

Что делать:
1. Нажмите `Проверить соединение`
2. Если Ollama не готов, нажмите `Автонастройка Ollama`
3. После завершения снова нажмите `Проверить соединение`
4. При необходимости нажмите `Обновить список моделей`

Как понять, что экран в порядке:
- в правом верхнем углу блока виден зелёный статус
- `Endpoint` показывает успешную проверку
- `Модель` показывает `mistral:instruct`
- `Время отклика` не пустое

Чего не стоит делать:
- не считать экран рабочим только по зелёному цвету
- не считать модель доступной без успешной проверки списка моделей и endpoint

Другие внутренние разделы настроек тоже рабочие:
- `Общие` для темы, стартового экрана и поведения первого запуска
- `Документы` для папки импорта и предпочтительного формата
- `Тренировка` для режима по умолчанию, профиля повторения и размера очереди
- `Данные` для путей и резервной копии SQLite
- `Продвинутые` для audit/docs и запуска `check_ollama.ps1`

## Экран 2. Импорт документов

Откройте `Импорт документов`.

![Импорт](screenshots/import.png)

Главный сценарий релиза:
- импорт одного большого `DOCX`

Поддерживаемый второй сценарий:
- `PDF`

Что произойдёт после выбора файла:
1. текст извлечётся
2. приложение попробует найти разделы и билеты
3. построит карту знаний
4. сохранит результат в базу
5. покажет summary и что делать дальше

Что смотреть в summary:
- имя документа
- число созданных билетов
- число разделов
- предупреждения
- `LLM assist: да/нет`

Если импорт идёт долго, приложение должно оставаться отзывчивым и показывать состояние импорта. Белое окно с `Не отвечает` это дефект, а не нормальное поведение.

Если видите warnings, это не обязательно поломка.
Чаще это знак, что структура в документе была неидеальной, и часть разбора шла через fallback.

## Экран 3. Библиотека

После импорта откройте `Библиотеку`.

![Библиотека](screenshots/library.png)

Что тут важно:
- слева список документов
- по центру детали выбранного документа
- справа общая статистика
- снизу режимы тренировки

Как пользоваться:
1. выберите нужный документ
2. проверьте, что справа корректно отображаются разделы и билеты
3. нажмите нужный режим тренировки или откройте раздел `Тренировка`

Если локальный ИИ ещё не готов или билеты ещё не загружены, в верхней части экрана появится onboarding-блок с честным следующим шагом.

## Экран 4. Тренировка

Откройте `Тренировку`.

![Тренировка](screenshots/training.png)

Экран состоит из двух частей:
- слева адаптивная очередь
- справа текущая сессия ответа

Как пройти минимальную сессию:
1. выберите билет из очереди
2. выберите режим тренировки
3. напишите ответ
4. нажмите `Проверить ответ`
5. прочитайте feedback
6. если есть follow-up вопросы, ответьте на них следующей попыткой

Если билет не выбран или ответ пустой, приложение должно показать честную ошибку. Ложный success в этом месте считается дефектом.

После каждой проверки система обновляет:
- слабые места
- профили навыков
- очередь повторения
- общую статистику

## Экран 5. Статистика

Откройте `Статистику`.

![Статистика](screenshots/statistics.png)

Там можно быстро понять:
- насколько вы готовы в среднем
- что у вас проседает по навыкам
- какие слабые места уже обнаружены
- какие сессии были последними

На что смотреть в первую очередь:
- слабые стороны по микронавыкам
- открытые слабые места
- последние низкие результаты

## Экран 6. Карта билета

Экран `Билеты` показывает внутреннюю структуру материала.

![Билеты](screenshots/tickets.png)

Зачем он нужен:
- увидеть не только текст, но и атомы знаний
- понять, какие части билета уже выделены
- посмотреть связи, навыки и упражнения

## Типовой рабочий маршрут

Если нужно быстро начать готовиться:
1. проверить `Ollama`
2. импортировать большой `DOCX`
3. открыть `Библиотеку`
4. перейти в `Тренировку`
5. пройти 3-5 попыток
6. открыть `Статистику`
7. повторить цикл на следующий день

## Частые проблемы

### Статус есть, но ИИ по факту не работает

Смотрите не на цвет, а на факты:
- есть ли модель в диагностике
- есть ли время отклика
- проходит ли `scripts\check_ollama.ps1`

### Импорт прошёл, но билетов мало

Причины:
- в исходном документе слабая структура
- билеты плохо размечены
- часть материала ушла в fallback

Что делать:
- попробовать более чистый `DOCX`
- упростить нумерацию билетов в документе
- снова импортировать файл

### После проверки ответа нет полезного эффекта

Проверьте:
- действительно ли был выбран билет
- был ли непустой ответ
- изменилась ли статистика после проверки

## DLC teaser

В интерфейсе есть отдельный teaser:
- `DLC: Подготовка к защите магистерской`

Это пока не рабочий модуль.
Он только показывает направление следующего расширения:
- загрузка текста магистерской
- разбор структуры доклада
- тренировка ответов на вопросы комиссии
- режимы `научрук` и `оппонент`
- short defense outline

## macOS

На macOS в репозитории подготовлены код, скрипты и инструкция. Финальный ручной runtime smoke-run должен выполняться на реальном Mac отдельно.

Практический путь:
1. сначала попробовать запуск из исходников через `python3 main.py`
2. затем при необходимости собрать `.app` через `bash scripts/build_mac_app.sh`
3. для локального Ollama использовать `bash scripts/setup_ollama_macos.sh` и `bash scripts/check_ollama_macos.sh`

Если macOS не даёт открыть unsigned app:
- откройте приложение через `Open` в Finder
- либо снимите quarantine:

```bash
xattr -dr com.apple.quarantine dist/TicketExamTrainer.app
```

По официальной документации Ollama на macOS данные и модели лежат в `~/.ollama`.
Источник: [Ollama macOS docs](https://docs.ollama.com/macos)
